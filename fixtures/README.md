# Fixtures — synthetic dry-run sample content

Generated by `python scripts/make_fixtures.py`. Used for discretionary fixture dry runs
(constitution v2.0.0 Principle V): `newsletter run --month 2026-05 --source fixtures`
(use `config-dryrun/config.yaml` to run offline against the mock provider).

## Expected content per file

| File | Type | Expected pipeline behavior |
|------|------|---------------------------|
| `2026-05-architecture-sync/transcript.docx` | Teams-style transcript | Detected as `transcript` (speaker + timestamp headers). Technical turns (API gateway migration, JWT validation, Redis caching results, per-region deployment decision, action items) are **included**; small talk (weekend, barbecue, office party) is **excluded** as non-technical. Speaker names are mapped to roles. |
| `2026-05-architecture-sync/platform-deck.pptx` | Slide deck | One chunk per slide with slide-number provenance. All four slides are technical and **included** (slide 4 carries presenter notes). |
| `2026-05-architecture-sync/caching-design-note.pdf` | PDF document | One chunk with page-1 provenance. Technical (caching strategy) — **included**. |
| `2026-05-architecture-sync/deployment-notes.txt` | Text note | Paragraph chunks with line ranges. Canary/rollback paragraphs **included**; the team-lunch reminder **excluded**. |
| `2026-05-team-update/team-update.docx` | Generic (non-transcript) .docx | Detected as `document` (no transcript pattern), chunked by heading. Farewell picnic and social corner **excluded**; the CI-runner build-time note **included**. |
| `corrupt.docx` | Corrupt file | Parsing fails → **skipped and reported** with a reason in `review/summary.md` (FR-004); the run continues. |
| `README.md` (this file) | Markdown | Ingested as text — harmless scan noise that exercises paragraph chunking. |
